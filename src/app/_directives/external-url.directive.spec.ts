import { ExternalUrlDirective } from './external-url.directive';

describe('ExternalUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ExternalUrlDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
